method sync(x: Int, flag: Bool) returns (r: Int) {
    if (flag) {
        r := 3 * x
    } else {
        var y:Int 
        y := x + x
        r := x + y
        r := r + 1
        r := r + 2
    }
    r := r * 2
}